# aws_glue_local_environment
Docker container to quickly set up your local aws glue development environment
Currently contains all the necessary packages for connecting to postgres and oracle databases
